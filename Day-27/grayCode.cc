#include <vector>
#include <iostream>
#include <algorithm>

using std::vector;
using std::max;
using std::min;
using std::sort;
using std::swap;
using std::reverse;

void 

int main(int argc, char const *argv[])
{
    int n=0;
    std::cin>>n;

return 0;
}